<?xml version="1.0" encoding="UTF-8" ?>
<Shell x:Class="AndroidIconIssues.AppShell"
       xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:AndroidIconIssues"
       Shell.FlyoutBehavior="Disabled"
       Title="AndroidIconIssues">

    <ShellContent Title="Home Page"
                  ContentTemplate="{DataTemplate local:HomePage}"
                  Route="HomePage" />

    <ShellContent Title="Students Page"
                  ContentTemplate="{DataTemplate local:StudentsPage}"
                  Route="StudentsPage" />
</Shell>